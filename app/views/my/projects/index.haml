%h2 Listing your projects

.well
  = link_to '+ New Project', [:new, :my, :project], class: 'btn btn-small btn-primary pull-right'

%ul
  -@projects.each do |project|
    %li.project
      = link_to project.title, [:edit, :my, project]
      %p= project.teaser
